{
  "name": "@throughline/agent-worker",
  "version": "0.0.0",
  "private": true,
  "description": "Agent worker process skeleton; extraction and ChangeSet behavior are deferred.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@throughline/observability": "workspace:*"
  }
}
