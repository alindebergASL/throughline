{
  "name": "@throughline/connector-worker",
  "version": "0.0.0",
  "private": true,
  "description": "Connector worker skeleton; provider integrations and MCP adapter are deferred.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@throughline/observability": "workspace:*"
  }
}
