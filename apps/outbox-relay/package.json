{
  "name": "@throughline/outbox-relay",
  "version": "0.0.0",
  "private": true,
  "description": "Tenant-scoped Foundation transactional outbox relay.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.840.0",
    "@throughline/authorization": "workspace:*",
    "@throughline/core-types": "workspace:*",
    "@throughline/db": "workspace:*",
    "@throughline/observability": "workspace:*",
    "@throughline/tenancy": "workspace:*"
  }
}
