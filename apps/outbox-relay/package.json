{
  "name": "@throughline/outbox-relay",
  "version": "0.0.0",
  "private": true,
  "description": "Transactional outbox relay skeleton; publishing semantics are deferred.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@throughline/observability": "workspace:*"
  }
}
