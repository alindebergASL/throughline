{
  "name": "@throughline/db",
  "version": "0.0.0",
  "private": true,
  "description": "Drizzle/PostgreSQL package placeholder; schemas and migrations start in later waves.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:security": "vitest run src/security.spec.ts"
  },
  "dependencies": {
    "@throughline/core-types": "workspace:*",
    "@throughline/tenancy": "workspace:*",
    "drizzle-orm": "^0.44.4",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.31.4"
  }
}
