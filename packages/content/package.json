{
  "name": "@throughline/content",
  "version": "0.0.0",
  "private": true,
  "description": "B1 collaborative content and deterministic manual SourceArtifact capture.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@throughline/core-types": "workspace:*",
    "@throughline/db": "workspace:*"
  }
}
