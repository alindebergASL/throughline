{
  "name": "@throughline/truth-ledger",
  "version": "0.0.0",
  "private": true,
  "description": "Durable, permission-aware B2 Claim and AcceptedFact write boundary.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@throughline/authorization": "workspace:*",
    "@throughline/core-types": "workspace:*",
    "@throughline/db": "workspace:*"
  },
  "devDependencies": {
    "@throughline/tenancy": "workspace:*"
  }
}
