{
  "name": "@throughline/truth-ledger",
  "version": "0.0.0",
  "private": true,
  "description": "B2 Truth Ledger contracts, verified evidence admission, and pure trust rules.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@throughline/core-types": "workspace:*"
  }
}
