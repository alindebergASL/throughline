{
  "name": "@throughline/account-operations",
  "version": "0.0.0",
  "private": true,
  "description": "First-party Account & Partner Operations package placeholder.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@throughline/core-types": "workspace:*",
    "@throughline/work-graph": "workspace:*"
  }
}
