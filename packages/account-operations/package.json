{
  "name": "@throughline/account-operations",
  "version": "0.0.0",
  "private": true,
  "description": "Typed Account Operations commands for the manual B1 trusted-memory capture loop.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@throughline/authorization": "workspace:*",
    "@throughline/content": "workspace:*",
    "@throughline/core-types": "workspace:*",
    "@throughline/db": "workspace:*",
    "@throughline/domain-profiles": "workspace:*",
    "@throughline/work-graph": "workspace:*",
    "zod": "^4.0.14"
  }
}
