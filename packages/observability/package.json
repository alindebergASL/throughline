{
  "name": "@throughline/observability",
  "version": "0.0.0",
  "private": true,
  "description": "Typed request correlation and W3C OpenTelemetry propagation helpers.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@opentelemetry/core": "^2.2.0",
    "@throughline/core-types": "workspace:*"
  }
}
