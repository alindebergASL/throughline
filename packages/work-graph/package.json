{
  "name": "@throughline/work-graph",
  "version": "0.0.0",
  "private": true,
  "description": "B1 Organization, Initiative, Activity, and Relationship work graph.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@throughline/core-types": "workspace:*",
    "@throughline/db": "workspace:*"
  }
}
